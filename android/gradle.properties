org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Disable Kotlin incremental compilation to dodge the "Storage ... already registered"
# "Could not close incremental caches" bug in the Kotlin daemon on Windows when shared
# cache files (build/.../caches-jvm) are touched across builds. Non-incremental is slower
# but reliably succeeds for plugin compilation.
kotlin.incremental=false
